apiVersion: v1
kind: Service
metadata:
  name: audit-consumer-service
  namespace: events
  labels:
    name: audit-consumer-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  clusterIP: None
  selector:
    app: audit-consumer