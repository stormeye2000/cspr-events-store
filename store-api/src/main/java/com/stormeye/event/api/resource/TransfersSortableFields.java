package com.stormeye.event.api.resource;

// Suppress: Rename this constant name to match the regular expression ‘^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$’
@SuppressWarnings("java:S115")
enum TransfersSortableFields {
    timestamp,
    fromAccount,
    toAccount,
    transferId,
    amount
}
